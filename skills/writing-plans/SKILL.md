---
name: writing-plans
description: 当你有规格说明或需求用于多步骤任务时使用，在动手写代码之前；计划必须包含验证预算、测试策略、检查点和用户验收路径
version: "1.0.0"
license: MIT
metadata:
  hermes:
    tags: [planning, documentation]
---

# 编写计划

编写可交给另一个工程师或 agent 执行的实现计划。计划要锁定架构、文件边界、任务顺序、验证预算、测试策略和检查点。

**开始时宣布：** "我正在使用 writing-plans 技能创建实现计划。"

**必需子技能：** 先使用 `superpowers:testing-policy` 为每个任务选择验证预算和验证等级。

**计划保存位置：** `docs/superpowers/plans/YYYY-MM-DD-<feature-name>.md`
用户指定位置时，以用户指令为准。

## 计划原则

- 每个任务产出一个有意义的、可审查的变更。
- 任务粒度按行为或集成边界划分，不按“写测试/跑测试/写代码”机械拆分。
- 小任务共享验证检查点；重型编译/测试不得按任务重复运行。
- 高风险任务必须即时验证，但仍要写明重型验证预算。
- 不要求每个任务都 TDD；由 `testing-policy` 判断 A/B/C/D。
- 每个计划必须说明哪些验证立即执行，哪些合并到检查点执行，哪些交由用户验收。

## 文件结构

在任务列表之前，列出将创建或修改的文件及职责。只列执行计划所需的信息：

- 文件职责和边界
- 关键接口或数据流
- 需要遵循的既有模式
- 需要避免的耦合或过度构建

## 计划头部

```markdown
# [功能名称] 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:testing-policy 选择验证预算和测试策略；使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 执行。步骤使用复选框（`- [ ]`）语法跟踪进度。

**目标：** [一句话描述要构建什么]

**架构：** [2-3 句话描述方案]

**技术栈：** [关键技术/库]

**验证预算总览：** [是否允许重型验证、最多几次、超时后如何处理]
**验证策略总览：** [哪些任务立即验证，哪些任务在检查点合并验证，哪些交由用户验收]

---
```

## 任务模板

````markdown
### 任务 N：[任务名称]

**文件：**
- 创建/修改：`exact/path/to/file`
- 测试：`tests/exact/path/to/test`（如适用）

**测试策略：** A 强制 TDD / B 实现优先目标验证 / C 轻量核查 / D 交付用户验证
**策略理由：** [为什么这个等级足够；如无需红灯，写清楚原因]
**验证预算：** [是否允许重型验证；最多几次；已知慢命令如何处理]
**验证检查点：** 立即执行 / 并入检查点 M / 完成前统一执行
**用户验收路径：** [接口、页面、输入数据、预期结果；不适用则写“无”]
**可合并验证任务：** [任务编号列表，或“无”]

- [ ] 实现 [具体行为或结构变更]

[必要时给出关键代码片段、接口签名或伪代码。不要写占位符。]

- [ ] 按策略记录验证证据和预算使用

运行：`exact command`
预期：`具体结果`，或说明轻量核查/用户验收方式。
````

## TDD 任务写法

只有 `testing-policy` 判定为 A 时，才在任务内写红-绿步骤：

```markdown
**测试策略：** A 强制 TDD
**策略理由：** 修复支付状态回归，必须先证明测试能捕获原始 bug。
**验证预算：** 允许 1 次红灯和 1 次绿灯目标测试；不运行完整套件。
**验证检查点：** 立即执行

- [ ] 写回归测试，测试必须执行到断言并因原始症状失败
- [ ] 运行 `npm test tests/payment-status.test.ts -t "keeps paid order closed"`，预期 FAIL，原因是订单状态被错误重开
- [ ] 实现最小修复
- [ ] 重新运行同一测试，预期 PASS
```

如果红灯需要重型编译、复杂 mock/stub、长时间运行，或只会得到“方法未定义”/编译失败，不要写成 TDD 任务；使用策略 B，先实现再做目标验证或用户验收。

## 禁止占位符

不要写：
- "待定"、"TODO"、"后续实现"
- "添加适当的错误处理"
- "为上述代码编写测试"
- "运行相关测试"
- "确认功能正常"
- "类似任务 N"

必须写具体文件、命令、预期结果和策略理由。

## 自检

计划完成后检查：

1. **需求覆盖：** 每个需求都能映射到任务。
2. **策略覆盖：** 每个任务都有测试策略、理由、验证预算和验证检查点。
3. **无意义红灯：** 没有把未定义方法、缺失 import、编译失败写成有效红灯。
4. **验证预算：** 重型编译/测试没有按小任务重复运行。
5. **执行完整：** 没有占位符、隐含依赖或不明确命令。

## 执行交接

保存计划后，提供执行选项：

**"计划已完成并保存到 `docs/superpowers/plans/<filename>.md`。两种执行方式：**

**1. 子代理驱动（推荐）** - 每个任务调度一个新子代理，按检查点合并验证，并进行规格/质量审查

**2. 内联执行** - 在当前会话中使用 executing-plans 执行任务，按计划中的验证检查点推进

**选哪种方式？"**
